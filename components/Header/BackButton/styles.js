import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  backButtonContainer: {
    position:'absolute',
    top: 21,
    left: 9,
    zIndex: 102,
  },
  backButton: {
    color: '#fff',
    fontSize: 38,
    opacity: 0,
  }
});
