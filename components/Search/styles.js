import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  searchText: {
    justifyContent: 'center',
    flex: 1,
    fontSize: 16,
    fontWeight: '600',
    color: '#fff',
    height: 60,
  },
  searchIcon: {
    justifyContent: 'center',
    fontSize: 18,
    color: '#fff',
    marginRight:5,
  },
})