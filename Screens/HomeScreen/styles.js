import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  titleContainer: {
    alignItems: 'flex-start',
  },
  title: {
    marginBottom: 10,
    fontSize: 26,
    fontWeight: '700',
  },
});
