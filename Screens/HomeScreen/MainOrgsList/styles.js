import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  mainOrgsListContainer: {
    backgroundColor: 'white',
    padding: 20,
    marginTop: 20,
    borderRadius: 15,
    shadowColor: '#000',
    shadowOffset: { width: 0, height: 1 },
    shadowOpacity:  0.12,
    shadowRadius: 20,
    elevation: 10,
  },
  orgContainer: {
    marginBottom: 15,
  },
  lastOrgContainer: {
    marginBottom: 0,
  }
})
